# VectorView config — copy from meta-bridge .env or set here
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Port to serve on
VECTORVIEW_PORT=7433

# Max points to pull per collection (PCA is O(n*dim), keep reasonable)
VECTORVIEW_MAX_POINTS=2000
